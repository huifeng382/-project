# GNN 项目要求文件（对 AI 助手，贯穿整个项目）

> 本文件是 GNN 项目（含 NetlistOpt / Rust 集成、数据、文档、脚本）的**最高优先级要求文件**。
> **任何在本项目下开启的新对话**：只要用户输入触发文本（§3）、或提及「要求文件 / REQUIREMENTS / 项目要求 / 版本号」，
> **必须先读取本文件并严格遵守全部条款**。本文件未覆盖之处，遵循 `docs/PROJECT_LOG.md`（15.2.5 文件归类等）与 `docs/GIT_PUSH.md`（推送方法）。
> **写代码 / 改数据缓存训练管线 / 部署服务器前必读**：`docs/GNN_CODING_LESSONS.md`（编码踩坑实录：内存/缓存/进程/验证方法，16.6.1 起）。编码教训统一记在该文件，本文件不收录。

---

## 1. git 提交 / GitHub 推送的版本号命名规范（强制）

- 版本号格式：**大版本.小版本.更小版本**，如 `15.2.16`。
- **每次提交 = 一个新版本**：提交后版本号必须往后递推；commit message = **版本号 + 简短文字描述**（一句话说明改动，**最长不超过 20 个汉字**，2026-09-03 定），如：
  `git commit -m "15.2.19 - 更新触发文本示例"`。
- **⚠️ 含 merge 提交在内，一律带版本号（2026-08-28 教训）**：merge 提交也必须按 §1 命名（如 `16.2.2 - merge 远程生成方提交`），**不得**用 `merge: ...` 等无版本号的标题。曾把 merge 提交 `11fef19` 命名为 `merge: 合并远程生成方提交…`（无版本号）违反本规范；该提交已推送无法改名，仅记录教训，**后续所有提交（含 merge）严格执行**。
- 递推规则（按本次改动的**最高级别**递推）：

| 级别 | 版本号递推 | 触发条件 |
|---|---|---|
| **大版本**（major） | 大版本号 +1，小/更小清零（如 10.1.0 → **11.0.0**） | 代码**大幅度**修改；**数据**有改动；代码修改带来了**大幅度的改善** |
| **小版本**（minor） | 小版本号 +1，更小清零（如 10.1.0 → **10.2.0**） | 代码**小幅度**修改；小幅度**加入新的代码文件** |
| **更小版本**（patch） | 更小版本号 +1（如 10.1.0 → **10.1.1**） | 加入**临时文件**；**修改 bug**；**修改文本文件**（含本文档） |

- 一次提交同时含多级改动时，取最高级别递推。
- **⚠️ 版本号必须单调递增，绝不回退（2026-08-28 教训）**：大版本升过后（如 16.0.0），后续提交从该版本继续（16.0.1 / 16.1.0 / 17.0.0），**不得再使用低于已用过的版本号**（曾错把 16.0.0 之后的提交标成 15.5.5/15.6.0，已重排修正）。提交前先看 `git log --oneline -3` 确认当前最新版本号。
- **版本号延续 GitHub 既有编号（不重新起算；`10.0.0` 仅是格式举例）**：
  最近一个按本规范命名的提交 = **`15.2.16`**（commit `f8064ce`，2026-08-26，已推送 GitHub）。
  **下一提交从 `15.2.17` 起**，按上表递推规则递增；当前版本以 §5 版本记录最新行为准。
- 注：2026-08-26 曾有 3 个未按版本号命名的过渡提交（`864b6e4` feat / `3b7727d` chore / `70d34a0` docs，均已推送），**不计入编号**；自 15.2.17 起严格执行。
- 推送方法：`docs/GIT_PUSH.md`（`-c http.sslBackend=openssl` + 凭据管理器 + 沙箱提升）。

## 2. 仓库布局（长期）

| 仓库 | 位置 | 远端 | 规则 |
|---|---|---|---|
| **project**（GNN 项目） | 工作区根目录 | `github.com/huifeng382/-project.git`，分支 `10.3.3-fix-earlystop` | 本地 + GitHub |
| **NetlistOpt** | `NetlistOpt/`（独立 git 仓库，父仓库已 ignore） | **无** | **仅本地 git，不推远端**（用户明确要求） |

## 3. 触发文本（记入要求模式）

- **触发文本（输入这一句即可）：「记入要求文件 GNN_PROJECT_REQUIREMENTS.md」**
  - 短形式（等价）：「记入要求文件」/「写入要求文件」/「记入要求」。
- 用户输入触发文本后：**用户之后所说的话，凡属于要求/规范/约束/长期决策类的，一律追加写入本文件**（`docs/GNN_PROJECT_REQUIREMENTS.md`），
  按 §1 以 patch 级别提交（如 `15.2.x+1 - 要求更新: ...`），并在回复中回显确认已记录。
- **新对话中只要出现触发文本，立即进入本模式**，无需其它解释。

## 4. 长期规则汇总（引用，避免重复）

- **文件归类**：文档 → `docs/`；诊断脚本 → `scripts/diag/`；报告 → `reports/`；根目录不留散文件（`PROJECT_LOG.md` 15.2.5）。
- **数据合规记录**：版本记录式（`docs/DATA_SPEC_V2_ISSUES.md`，V1.0~V3.1 等）。
- **Rust 集成记录**：`docs/GNN_RUST_DATA_DIFF.md` 第十节（10.1~10.8）。
- **推送方法**：`docs/GIT_PUSH.md`。
- **记录风格**：凡问题清单/要求类文档，优先采用「版本记录式」（版本/日期/状态/摘要表）。
- **训练数据默认（2026-08-28 定，16.11.4 更新）**：**`batch_v2_full` + `batch_v2_rest` + `batch_v2_m4` 三组**（未另行指定时；m4 = V3.2 五形状补充数据，2026-08-31 起默认并入）。`batch_v2_io` 作验证/开发集，不默认入训练；rest 的 84 单变体 + 90 <10 变体组建议剔除。
- **测试结果记录规则（16.11.16 定，强制，每次自动执行）**：① **每次训练结果（GNN test 评估 + Rust 集成验证）→ 记入 `docs/PROJECT_LOG.md`**（16.x 记录区，含版本/变体/特征/Test Median/选择遗憾/recall/Rust 各指标）；② **检查/分析类结论（serve 失配归因、分布差异、可行性验证等）→ 记入 `docs/GNN_RUST_DATA_DIFF.md`**；③ 版本号随提交递增（§1）。
- **SSH 工具不可用时的兜底工作流（2026-08-28 定）**：如果 ssh 工具（ssh_exec/ssh_upload 等）不能使用，则**在本地修改代码 → push 到远程（GitHub）→ 服务器 `git pull` → 用 `setup_exp.sh` 启动**。不要卡在无法直连服务器上。
- **服务器启动实验必须用 `setup_exp.sh`（2026-08-28 定，强制）**：所有训练/实验的服务器启动一律通过 `bash setup_exp.sh <variant>`（clone 分支 → 变体 sed → `CACHE_DIR="cache107$V"` → `OMP_NUM_THREADS=6 nohup ~/venv/bin/python3 -u main.py > train107$V.log 2>&1 &`）。
  **禁止手写 for 循环 / 手动 nohup 逐 run 启动**（曾因手动 for 循环启动 4 个 v3 run 违反本规范；如需给 run 目录同步代码/保留缓存等 setup_exp.sh 不直接支持的操作，也应先说明方案经用户同意后，仍以 `setup_exp.sh` 为主体方式执行）。

- **数据相关问题以 DIFF 文档为参考（2026-09-03 定，强制）**：回答任何与数据相关的信息（数据分布/差异/归因/规格数值/实测结论等）时，**以 docs/GNN_RUST_DATA_DIFF.md（DIFF 文档）为参考基准**——它集中了 Rust/训练数据的全部实测与归因（§10-§14）；引用数值/结论前先查 DIFF，避免凭记忆或重复分析。**以 §14 数据支持审计的标注为准**：标「⚠/待复核」的内容（如 §13 v2iaa42m4 Rust 数字）不作定论引用；新增数据相关结论也优先记录于此（§4 测试结果记录规则同）。

- **SSH 工具当前不可用，服务器操作一律「给命令、用户执行」（2026-09-03 定，补充 16.2.1 兜底工作流）**：AI 环境当前**无法使用 ssh 工具**（ssh_exec/ssh_upload 等均不可用）；此后所有 SSH/服务器相关操作（查进程/看日志/启停/serve/Rust shadow/代码同步等）一律改为 **AI 只给出复制即用命令 → 用户自行执行并回贴输出 → AI 基于输出分析**；**AI 不直接执行 ssh**。命令须遵守 §6.1 命令安全规范（无自杀式通配符、日志名核对、OMP_NUM_THREADS 等）。与 16.2.1（本地改 → push → 服务器 git pull → setup_exp.sh）互补：能 git pull 的用 16.2.1 路径，需即时查/启/同步时用本「给命令」路径。ssh 工具恢复后再评估是否改回。

## 5. 行为约束（对 AI 助手，强制）

- **方案先行，先问后改**：每次对某个问题给出解决方案时，**先给出方案（含改动清单/预期影响），不要自动执行修改**，等待用户明确同意后再动手。
- 例外：用户已明确指示执行的操作（如「直接跑」「修改」），或纯只读操作（查询、分析、读取），不在此限。
- 若方案涉及代码/文件改动、重跑实验、提交推送，一律先列清单询问。
- **长任务汇报规则**：用 SSH 启动测试/训练后，若预计等待 **>30 分钟**，**不要定期汇报进度**——只需确认程序已启动运行，并**附上检查命令**让用户自查；仅在任务完成时汇报结果。

- **数据分析必须先完整验证再下结论（16.11.25 定，强制）**：给出对数据的分析/结论前（规模/深度/覆盖率/分布/归因/可行性等），**先完整检查一遍、确认正确，再下结论**；不得凭抽样印象、单一口径、未核对的数据随口断言：
  ① **全量核对**：能用全量数据算的不用抽样（抽样须标注并复核全量）；引用任何数值前先复算；
  ② **口径确认**：结论所用量纲/口径（如深度 = X_ 宏级链 vs .tl 级联、晶体管 vs 门数）必须与对比对象（训练侧/Rust 侧）一致并写明，不一致不得直接比较；
  ③ **交叉验证**：关键结论用两种独立方式/脚本核对（如模板级 + 候选级），矛盾时以更贴近使用域的实测为准；
  ④ **自查清单**：下结论前自查——是否被全量数据支持？口径是否一致？是否与既有记录冲突？冲突时先复核旧记录，不得直接否定或沿用；
  ⑤ **证据不足标「待复核」，不下定论**：证据不足时明确标注「待复核」，不写进 spec/结论正文当定论（教训：16.11.23 未核对 Rust 候选实测就按模板级深度把 DEPTH_MIX 定为「深度 OOD」并写入锚定表 ★ 深度档，16.11.24 被 1135 个 dut 网表候选实测（0% >6）推翻，需撤回并修正）。

## 6. SSH 插件复制粘贴补丁（@linxin666/dsh-ssh，2026-08-26 实测定稿）

**问题**：插件内嵌 xterm 把裸 Ctrl+C 硬编码为发送 ^C 并 preventDefault，Chrome 又不为 Ctrl+Shift+C 触发原生 copy → 终端无法复制；Ctrl+V 也被吞。

**补丁**：`~/.dsh/patch-dsh-ssh-copy.mjs`（幂等，按锚点 `new import_xterm.Terminal({` 定位，不依赖行号）→ 给终端挂 `attachCustomKeyEventHandler`：
- Ctrl+C / Ctrl+Shift+C（**有选中**）→ 复制选中文本，不发中断
- Ctrl+C（**无选中**）→ 照常发送 ^C 中断
- Ctrl+V（无 Shift）→ 放行浏览器原生粘贴

**维护规则**：
- **插件升级/重装会覆盖本补丁** → 升级后重跑 `node ~/.dsh/patch-dsh-ssh-copy.mjs` 恢复，再**硬刷新页面（Ctrl+Shift+R）**生效。
- 写 `~/.dsh/`（工作区外）需 `danger-full-access` 权限。
- 备份：`client.js.dshcopy.bak`。

### 6.1 命令安全（踩坑实录，SSH 操作通用）

1. **禁止 `pkill -f 'xxx'` / `pgrep -f 'xxx'` 匹配自身**：命令命令行里含 'xxx' 字样 → 正则匹配到执行命令的 shell 自己 → **自杀断连**。正确做法：
   - 精确进程名：`pkill -9 -x <进程名>`
   - 括号技巧：`pgrep -f '[t]l_opt_shadow_batch'`（正则不匹配字面 `[t]...`，只匹配真实进程）
   - 或直接用 PID：`kill -9 <pid>`（`ss -ltnp` / `ps aux` 查）
2. **日志文件名必须核对**：循环变量拼日志名时易错（如 `train_${d#v2}.log` vs `train_$d.log`——run 目录名与日志名前缀可能不同）。复制粘贴/生成命令前先 `ls` 确认实际文件名。
3. **长命令会被终端截断**：超长粘贴命令可能被截断 → 拆成短命令，或写成脚本文件上传再执行。
4. **长任务用 nohup**：`nohup ... > log 2>&1 &`，避免 SSH 超时/断连中断；启动后 `sleep` 几秒确认进程 + 日志头已写出再离开。
5. **给用户的检查命令必须复制粘贴即用**：无自杀式通配符、路径/文件名正确、输出明确（如 `echo "$d: $(...) epochs"` 带标签）。
6. **ssh_exec 超时**：单条命令默认有超时；重活放 nohup 后台 + 轮询日志，不在前台长 sleep。
7. **服务器训练必须设 `OMP_NUM_THREADS`（重要，2026-08-28 实测）**：torch 默认每进程用满**所有核**；多 run 并行（如 4 run）不设 → 4×24=96+ 线程挤 24 核 → load 飙到 40+、缓存构建和 epoch 全慢 3-5×（实测首建缓存 11h、epoch 迟迟不出）。**每 run 设 `OMP_NUM_THREADS=6`**（并发数 × 每 run 线程数 ≤ 核数；4×6=24 满负荷不抖动）。检查：`grep Threads /proc/<pid>/status`（应 ~19，非 33）。

## 7. 版本记录（§1 的演进跟踪）

| 版本 | 日期 | 类型 | 摘要 |
|---|---|---|---|
| 15.2.16 | 2026-08-26 | patch | （GitHub 既有）Rust 集成挂载：`gnn_shadow.rs` 移入 `NetlistOpt/src`，更新第十节引用（commit `f8064ce`）。 |
| 15.2.17 | 2026-08-26 | patch | 新增本要求文件：版本号规范（延续既有 15.2.x 编号，10.0.0 仅为格式举例）+ 触发文本「记入要求文件」+ 仓库布局；`PROJECT_LOG.md` 加必读指引。 |
| 15.2.18 | 2026-08-26 | patch | 触发文本规范化为「记入要求文件 GNN_PROJECT_REQUIREMENTS.md」（短形式「记入要求文件」等价）。 |
| 15.2.19 | 2026-08-26 | patch | 提交规范补充：版本号后跟**简短文字描述**（一句话，不要太长）。 |
| 15.3.2 | 2026-08-26 | patch | 新增 §5 行为约束：**方案先行、先问后改**——给出解决方案后不自动执行修改，先询问用户（只读/已明确指示除外）。 |
| 15.5.1 | 2026-08-26 | patch | §5 补充**长任务汇报规则**：SSH 启动测试后若等待 >30 分钟，不定期汇报进度，只确认程序在跑 + 附检查命令，完成时再汇报结果。 |
| 15.5.2 | 2026-08-26 | patch | §6 定为 **SSH 插件复制粘贴补丁**维护规范（@linxin666/dsh-ssh：xterm Ctrl+C/V 问题、补丁脚本、升级后重跑规则）+ 6.1 命令安全踩坑实录；版本记录顺延为 §7。 |
| 15.5.3 | 2026-08-26 | patch | 插件更新到 0.3.5 覆盖补丁，已重跑 `patch-dsh-ssh-copy.mjs` 恢复（验证锚点兼容）；记录「升级后重跑补丁」规则。 |
| 15.5.4 | 2026-08-28 | patch | §6.1 新增第 7 条：**服务器训练必须设 `OMP_NUM_THREADS`**（实测 4 run 不设导致 96+ 线程挤 24 核、load 40+、训练慢 3-5×；每 run 设 6）。 |
| 16.0.0 | 2026-08-28 | major | （数据改动）V3.1 满量数据交付：`batch_v2_rest`(35,183 电路) + full/io 重收 + sc_expansion 合并 23,278。数据在远程为生成方 `6402ff7`（本地曾 cherry-pick 为 `734590b`，重排后已随远端一致，不再本地重复提交）。 |
| 16.0.1 | 2026-08-28 | patch | （原误标 15.5.5，已重排）§4 新增**训练数据默认 = full+rest**（io 作验证集）；`_check_v3_data.py` 落 scripts/diag/。 |
| 16.0.2 | 2026-08-28 | minor | （原误标 15.6.0，已重排）`DATA_BATCHES` 默认 full+rest + `MIN_GROUP_SIZE=10` 组大小过滤（剔除低价值组）。 |
| 16.0.3 | 2026-08-28 | patch | §1 新增**版本号必须单调递增、绝不回退**（16.0.0 之后误标 15.5.5/15.6.0 的教训，已重排修正）。 |
| 16.2.1 | 2026-08-28 | patch | §4 新增**SSH 工具不可用的兜底工作流**：本地改 → push 远程 → 服务器 git pull → setup_exp.sh 启动。 |
| 16.2.2 | 2026-08-28 | patch | §1 新增**含 merge 提交在内一律带版本号**（merge `11fef19` 无版本号命名教训）；§4 新增**服务器启动实验必须用 `setup_exp.sh`**，禁止手动 for 循环/手动 nohup 启动。 |

| 16.11.25 | 2026-09-03 | patch | §5 新增**数据分析必须先完整验证再下结论**（全量核对 / 口径确认 / 交叉验证 / 自查清单 / 证据不足标「待复核」不下定论）——教训：16.11.23 未核对 Rust 候选实测就把 DEPTH_MIX 定为深度 OOD 写入锚定表 ★ 深度档，16.11.24 被 1135 个 dut 候选实测（0% >6）推翻。 |
| 16.11.31 | 2026-09-03 | patch | §4 新增**数据相关问题以 DIFF 文档为参考基准**（引用前先查，以 §14 审计标注为准，⚠ 项不作定论；新结论优先记录 DIFF）。 |
| 16.11.33 | 2026-09-03 | patch | §1 commit **说明字段限 ≤20 汉字**；§4 新增 **SSH 工具不可用→服务器操作一律「给命令、用户执行」**（补充 16.2.1 兜底工作流）。 |